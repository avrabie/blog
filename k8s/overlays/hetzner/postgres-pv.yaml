apiVersion: v1
kind: PersistentVolume
metadata:
  name: blog-postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/blog-data/postgres
  claimRef:
    namespace: blog-namespace
    name: postgres-pvc
